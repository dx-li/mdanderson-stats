# BOIN combination reference audit

The bounded fixtures in `tests/fixtures/boin-combination-*` are generated by
[`tools/reference_boin_combination.R`](../tools/reference_boin_combination.R)
with CRAN `BOIN` 2.7.2 and its `Iso` dependency in a task-local R library.
The downloaded package and app material stay under ignored `research/raw/`;
their source is not redistributed here.

## Pinned sources

| source | observed version/date | SHA-256 |
| --- | --- | --- |
| [BOINComb app](https://biostatistics.mdanderson.org/shinyapps/BOINComb/) | V2.6.4.0, updated 2025-11-20 | `f20a19de665d7dd5bf9998cdd3cb1f45b347a70da1c9e6d0b9125e7b7bdae786` (landing HTML) |
| [BOINComb Guide.pdf](https://biostatistics.mdanderson.org/shinyapps/BOINComb/Guide.pdf) | user guide | `745abf47dd30e073f346ddc7ca5688e3a62bc316d972b67f7c5962edf8432b68` |
| [BOINComb Doses.pdf](https://biostatistics.mdanderson.org/shinyapps/BOINComb/Doses.pdf) | dose help | `4f77f3e78dab3adf700a3c1144ac2df65edbfaefd3c48f3a228c33a891534d7e` |
| [BOINComb Probability.pdf](https://biostatistics.mdanderson.org/shinyapps/BOINComb/Probability.pdf) | probability help | `af465fae61d6644a0c72f9d3296e19659f343e7b0a7b4fa06637ecbecb96465c` |
| [BOINComb Safety.pdf](https://biostatistics.mdanderson.org/shinyapps/BOINComb/Safety.pdf) | safety help | `f1563f1db13a9642eb1575cd33b666028a9312c0ecba5d1ee5b12abd73604d5e` |
| [BOIN 2.7.2 source archive](https://cran.r-project.org/src/contrib/BOIN_2.7.2.tar.gz) | CRAN package | `be643939639ccc70dd236712047056e051f7aa019a7230d09caf1f646dd0060e` |
| [BOIN package paper](https://doi.org/10.18637/jss.v094.i13) | Yan et al., JSS 94(13), 2020 | see existing `docs/boin-sources.json` |

The package declares GPL-2 and imports `Iso`; the package is used as an
independent executable reference. The app page names Yanhong Zhou, Ruitao Lin,
Suyu Liu, and Ying Yuan. The app guide is the source for app-level workflow
claims; the R source is the source for numerical backend behavior.

## Which combination design is exposed

The app has two objectives. **Single MTD** uses the standard BOIN combination
method implemented by `next.comb()` and `select.mtd.comb(mtd.contour=FALSE)`.
**MTD contour** uses the waterfall method: `next.subtrial()` chooses a starting
dose and the next subtrial space, and each subtrial is conducted with ordinary
BOIN boundaries. The guide says the app also wraps these methods with optional
accelerated titration, a titration cap, and a 3+3 run-in (the latter is intended
for target 0.25). Those wrappers are app workflow behavior; they are not extra
movement rules in `next.comb()`.

## Standard BOIN combination method

`next.comb()` computes the ordinary BOIN boundaries using
`p.saf = 0.6*target`, `p.tox = 1.4*target`, `cutoff.eli = 0.95`, and the
uniform-prior safety probability `Pr(p > target | y+1,n-y+1)`. At the current
cell, escalation occurs when `y` is at or below the escalation boundary and
de-escalation when `y` is at or above the de-escalation boundary. It considers
only the two axis-adjacent moves `(j+1,k)` and `(j,k+1)` for escalation and
`(j-1,k)` and `(j,k-1)` for de-escalation. The candidate with the largest

```
Pr(lambda_e < p <= lambda_d | y + .5, n - y + .5) + .0005*n
```

is selected; an exact tie is resolved using R's `runif`. Escalation excludes
eliminated candidates and candidates blocked by empirical toxicity estimates
in the preceding part of their row or column. The original de-escalation path
does not filter an inherited elimination mask; Python excludes such candidates.
A southeast rectangle is eliminated after an observed cell crosses the safety
cutoff. The lowest-dose elimination, extra-safe rule, and precision stopping
rule leave the native `next_dc` unavailable; Python returns `next_dose=None`.

The movement fixture includes unequal neighboring information, an exact
posterior tie, retention, de-escalation, safety closure, extra-safe stopping,
and precision stopping. It is intentionally bounded and records the native
one-based dose coordinates.

## Selection and isotonic fitting

`select.mtd.comb()` uses a cross-shaped elimination closure: an eliminated cell
marks cells to its right in the same row and cells below it in the same column.
The scan stops at the first unsafe cell in each row.
The final working estimate is

```
raw = (y + .05) / (n + .1)
fit = Iso::biviso(raw, n + .1)
```

and the fitted matrix is rounded to two decimals **before** MTD selection and
`boundMTD` filtering. Untreated cells display `NA`. Selection adds a tiny
row-plus-column perturbation (`1e-5`) before computing distance to the target;
it does not change the displayed fit. `boundMTD=TRUE` rejects adjusted rounded
estimates above the de-escalation boundary. With
`mtd.contour=TRUE`, the package chooses one admissible dose per row and applies
the contour continuity rule.

`boin-combination-biviso.csv` preserves 17-significant-digit, unrounded
`Iso::biviso` fits for unequal grids and weights. `boin-combination-selection.csv`
covers single MTD, contour, cross closure, all-toxic, ties, untreated cells,
and the bound-MTD option.

## Waterfall, stopping, and simulation

`next.subtrial()` identifies the lowest-indexed occupied subtrial, performs a weighted
one-dimensional isotonic selection among its treated, noneliminated cells, and
returns the next subtrial's start and search space. The native documented
waterfall example is retained in `boin-combination-waterfall.csv`.

`get.oc.comb()` defaults to `ntrial=1000`, starts at `(1,1)`, uses complete
binomial cohort outcomes, and accepts `mtd.contour=FALSE` for standard BOIN or
`TRUE` for waterfall. The app's simulation tab lets users enter scenarios and
choose the number of simulations; the app guide does not change the underlying
R defaults. The bounded `boin-combination-simulation.csv` and
`boin-combination-simulation-summary.csv` fixtures use 20 trials with a fixed
seed to check simulation wiring without standing in for a high-precision
operating-characteristic estimate.

## Known source/backend differences

The native package has a selection edge-case defect: when the lowest dose is
eliminated, `select.mtd.comb()` prints the no-MTD message but can return an
error because `phat.out.noCI` was not created on that branch. The all-toxic row
in the selection fixture records this observed behavior. A compatible Python
implementation should expose a clean no-MTD result while retaining the native
safety decision; this row is a source-parity warning, not a statistical rule.

There is also a backend inconsistency: the standalone `select.mtd.comb()` path
rounds the isotonic fit before choosing an MTD, while the nested selector used
inside `get.oc.comb()` selects from its unrounded fit. The selection fixtures
and focused Python tests target the standalone app-selection path; operating
characteristic parity should treat the simulator's nested path separately.
The native simulator also omits the interactive helper's empirical escalation
blockers. Its enrollment stop requires a retention condition (including blocked
escalation at the grid boundary), whereas `next.comb()` stops unconditionally
at the current-dose enrollment limit. These are different executable contracts
within the same package.

The app guide describes accelerated titration and a 3+3 run-in that are app
wrappers around the standard method. They should not be inferred from
`next.comb()` itself. The published method and package code also use different
labels in places (`n.earlystop` precision stop versus app wording); fixtures
record the returned native values rather than UI text.
